# Example Environment Variables for Databricks MCP Server
# Copy this file to .env and fill in your values for local development.
# These variables are loaded by src/databricks_mcp/config.py using pydantic-settings.

# --- Databricks Connection ---
# Required: Your Databricks workspace URL (e.g., https://dbc-XXXX.cloud.databricks.com)
# The databricks-sdk will automatically look for this environment variable.
# DATABRICKS_HOST=""

# Required: Databricks authentication token (Personal Access Token recommended for simplicity)
# The databricks-sdk will automatically look for this environment variable.
# Alternatively, configure other databricks-sdk auth methods (OAuth, Azure CLI, etc.).
# See: https://databricks-sdk-py.readthedocs.io/en/latest/authentication.html
# DATABRICKS_TOKEN=""


# --- Server Configuration ---

# Optional: Set the logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Defaults to INFO if not set.
# LOG_LEVEL="INFO"

# Optional: Enable the potentially risky `get_secret` tool.
# Defaults to false if not set. Set to "true" to enable. Use with extreme caution.
# ENABLE_GET_SECRET="false" 